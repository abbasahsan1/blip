apiVersion: v1
kind: Service
metadata:
  name: content-ingest-service
  namespace: blipp
  labels:
    app.kubernetes.io/name: content-ingest-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: blipp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8001
      targetPort: 8001
  selector:
    app.kubernetes.io/name: content-ingest
