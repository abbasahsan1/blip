apiVersion: v1
kind: Service
metadata:
  name: blipp-app
  namespace: blipp
  labels:
    app.kubernetes.io/name: blipp-app
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: blipp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: blipp-app
