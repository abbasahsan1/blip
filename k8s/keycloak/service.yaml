apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: blipp
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/component: idp
    app.kubernetes.io/part-of: blipp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app.kubernetes.io/name: keycloak
