apiVersion: v1
kind: Service
metadata:
  name: feed-service
  namespace: blipp
  labels:
    app.kubernetes.io/name: feed-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: blipp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8002
      targetPort: 8002
  selector:
    app.kubernetes.io/name: feed
