apiVersion: apps/v1
kind: Deployment
metadata:
  name: feed
  namespace: blipp
  labels:
    app.kubernetes.io/name: feed
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: blipp
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: feed
  template:
    metadata:
      labels:
        app.kubernetes.io/name: feed
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: blipp
    spec:
      containers:
        - name: feed
          image: blipp-feed-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8002
          envFrom:
            - secretRef:
                name: blipp-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
