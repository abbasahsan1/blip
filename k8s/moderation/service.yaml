apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: blipp
  labels:
    app.kubernetes.io/name: moderation-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: blipp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8005
      targetPort: 8005
  selector:
    app.kubernetes.io/name: moderation
